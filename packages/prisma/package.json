{
  "name": "prisma",
  "version": "0.0.0",
  "main": "./index.ts",
  "types": "./index.ts",
  "scripts": {
    "db:generate": "prisma generate --schema=./schema.prisma",
    "db:push": "prisma db push --skip-generate"
  },
  "dependencies": {
    "@prisma/client": "^5.3.1",
    "zod": "^3.22.2",
    "zod-prisma": "^0.5.4"
  },
  "devDependencies": {
    "prisma": "^5.3.1"
  }
}
